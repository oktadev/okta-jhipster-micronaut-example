version: '2'
services:
  space-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/space/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=space
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
